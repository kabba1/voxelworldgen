import type { CityState } from "../types";

export const assignHousing = (state: CityState): CityState => state;
export const produceFood = (state: CityState): CityState => state;
export const updateAgentNeedsFromBuildings = (state: CityState): CityState => state;
